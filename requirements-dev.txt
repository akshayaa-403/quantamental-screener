# Minimal dependency set to import the tested modules and run the test suite.
# Deliberately excludes the heavy ML/UI stack (torch, transformers, streamlit,
# selenium, plotly, ...) which the tests do not exercise, so CI stays fast and
# reliable. Keep in sync with the imports reached from tests/.
pandas>=2.0.0
numpy>=1.24.0
yfinance>=0.2.28
requests>=2.31.0
ta>=0.10.0
tenacity>=8.0.0
redis>=5.0.0
fakeredis[lua]>=2.20.0
pydantic>=2.4.0
pydantic-settings>=2.14.1
pytest>=7.4.0
